import React from 'react';

const ReturnArrow = () => {
    return (
        <div>
            Back {/*Find or make a arrow icon and replace it here*/}
        </div>
    )
}

export default ReturnArrow;